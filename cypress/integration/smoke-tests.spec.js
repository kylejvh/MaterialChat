describe("Smoke tests", () => {
  context("With fresh state, user not registered or authed", () => {
    it("registers and authorizes new user", () => {
      // ...
    });

    it("creates new chatrooms", () => {});

    it("joins the first created chatroom", () => {});

    it("sends messages in the chatroom", () => {});

    it("joins the second, then third chatroom", () => {});

    it("deletes the second chatroom", () => {});

    it("sends messages in third chatroom", () => {});

    it("rejoins the first chatroom", () => {});

    it("changes username/password??", () => {});

    it("logs the user out", () => {});
  });
});
